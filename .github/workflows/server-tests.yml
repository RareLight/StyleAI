name: "Server tests"
on:
  pull_request:
    paths:
      - 'server/**'
      - 'plugin/**'
      - 'scripts/validate_lrc_plugin.py'
      - '.github/workflows/server-tests.yml'
  push:
    branches: [main]
    paths:
      - 'server/**'
      - 'plugin/**'
      - 'scripts/validate_lrc_plugin.py'
      - '.github/workflows/server-tests.yml'
jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - name: Sync dependencies (locked)
        working-directory: server
        run: uv sync --locked
      - name: Run pytest
        working-directory: server
        run: uv run pytest test/ -v --cov-fail-under=50
      - name: Validate Lightroom plugin
        run: python3 scripts/validate_lrc_plugin.py
